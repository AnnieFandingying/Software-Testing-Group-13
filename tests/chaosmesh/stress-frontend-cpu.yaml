apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: checkout-frontend-cpu-pressure
  namespace: default
  labels:
    test-suite: checkout-chaos
    scenario: frontend-cpu
spec:
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: frontend
  stressors:
    cpu:
      workers: 2
      load: 85
  duration: "5m"
