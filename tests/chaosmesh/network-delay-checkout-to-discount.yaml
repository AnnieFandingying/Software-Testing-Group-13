apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: checkout-to-discount-delay
  namespace: default
  labels:
    test-suite: checkout-chaos
    scenario: checkout-discount-latency
spec:
  action: delay
  mode: all
  selector:
    namespaces:
      - default
    labelSelectors:
      app: checkoutservice
  direction: to
  target:
    mode: all
    selector:
      namespaces:
        - default
      labelSelectors:
        app: discountservice
  delay:
    latency: "800ms"
    correlation: "50"
    jitter: "120ms"
  duration: "5m"
