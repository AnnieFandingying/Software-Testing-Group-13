apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: member-c-checkout-pod-kill
  namespace: default
  labels:
    role: member-c
    scenario: checkout-pod-kill
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: checkoutservice
